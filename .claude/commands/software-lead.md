Invoke the software-lead agent to orchestrate the development workflow for the following request: $ARGUMENTS

Use the Agent tool with subagent_type="software-lead" and pass the full request as the prompt. Include all relevant context from the current working directory, recent git state, and any requirement files in scope.
