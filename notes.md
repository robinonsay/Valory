1. ANTHROPIC_API_KEY should be a part of the admin config page instead of an env variable. This will make it more user friendly
2. Same thing with the brave api key
3. We should have explanations for each item in the admin config so a non-expert can understand how to configure the system
