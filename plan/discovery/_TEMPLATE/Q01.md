# Q01 — <the question, verbatim from frontier.json>

> Node: `<root | G<n> | G<n>-S<m> | G<n>-S<m>-T<k>>`  ·  Depth: `<n>`  ·  Weight: `<0.0–1.0>`

## Answer

<The concrete, decision-ready answer. Every claim is either grounded or flagged as an
assumption — the discovery-gate fails confident ungrounded claims.>

## Evidence

- `path/to/file.go:142` — <what it shows>
- `REQ-MODULE-007` — <what it constrains>
- <real doc / URL> — <what it establishes>
- **ASSUMPTION:** <anything not grounded, stated plainly so the gate and the planner see it>

## Outcome

Pick one:

- **Leaf** — no child question would change a build decision.
  `leaf_reason:` <why this answer is decision-stable>
- **Spawned children** — `Q<NN>, Q<NN>` (added to the frontier via `discovery.py add --parent`).
- **Pruned siblings** — `Q<NN>` subsumed by this answer (`discovery.py prune <id> --by <this>`).
