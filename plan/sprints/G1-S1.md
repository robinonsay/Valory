# G1-S1 — Delete fix + Oversight names (Sprint 25)

> Level 2, goal G1. Owned by `software-lead`. The active frontier.

## Scope

Two independent tracks. Track A (T1–T2) fixes the user-delete HTTP 500 caused by the
`agent_runs.course_id` RESTRICT FK and adds a regression test. Track B (T3–T4) replaces the
raw student ID in Course Oversight with the student's name, split backend/frontend per the
"split BE/FE" rule.

## Tasks (level 3)

| Task | Increment | Worker | Verifier (acceptance) | Depends on |
|------|-----------|--------|-----------------------|------------|
| G1-S1-T1 | Fix `Repository.DeleteStudent`: delete `agent_runs` (and other RESTRICT-linked children) before `courses`, inside the existing tx; preserve RLS/role correctness | senior-engineer | SQE + Systems Engineer | — |
| G1-S1-T2 | Regression test seeding a course with an `agent_runs` row + pipeline_events, then deleting the student and asserting success + full cascade | test-author | SQE | G1-S1-T1 |
| G1-S1-T3 | Backend: admin path of `ListCourses` returns `student_username` (+ email) via a `users` join; student path unchanged | junior-engineer | SQE + Systems Engineer | — |
| G1-S1-T4 | Frontend: `CourseOversightView` shows student name (ID secondary/tooltip); update header + types + component test | junior-engineer | SQE | G1-S1-T3 |

## Dependencies / sequencing

Track A and Track B are independent. T2 depends on T1; T4 depends on T3. Frontier on dispatch:
**T1 and T3 in parallel**.

## Requirements in scope

- Ask 1 governed by existing **REQ-USER-007** (deletion of personal data) — add an acceptance
  criterion that deletion succeeds for a student with completed generation runs.
- Ask 2 → new **REQ-FEADMIN-701** + a backend criterion on the course-list admin projection.

## Definition of done

All four tasks done, acceptance pass, RLS verified under `SET ROLE valory_app` (test DB
superuser masks RLS bugs — see memory `force-rls-superuser-test-masking`).
