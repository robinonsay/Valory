# G1-S1-T4 — Oversight view shows student name

> Level 4 task contract. Owned by `software-lead`. Worked example.

- **Worker:** junior-engineer
- **Verifier (acceptance gate):** SQE
- **Depends on:** G1-S1-T3
- **Artifact path:** `frontend/src/views/admin/CourseOversightView.vue` + types/test; summary under `plan/artifacts/G1-S1-T4/`

## Design (how — the mandate)

`CourseOversightView.vue` renders `course.student_id`
([frontend/src/views/admin/CourseOversightView.vue:130](../../frontend/src/views/admin/CourseOversightView.vue#L130)).
Once G1-S1-T3 returns `student_username` (+ email), render the **name** as the primary value and
demote the ID to a secondary/tooltip. Update the column header, the TypeScript types for the
course row, and the component test. Composition API with `<script setup>`; props/types typed; no
raw HTML rendering of the name (XSS).

## Requirements (what-for)

- **REQ-FEADMIN-701** — the oversight UI displays the student's name.

## Acceptance (done-criteria)

- [ ] Oversight table shows student name; ID is secondary (tooltip/secondary line).
- [ ] Column header + row types updated; component test asserts the name renders.
- [ ] `<script setup>` + typed props; name is not rendered as raw HTML.
- [ ] Touched functions carry `@{"req", ["REQ-FEADMIN-701"]}` annotations.
- [ ] `eslint` clean; `vitest run` passes.

## Worker return schema

```
task_id       G1-S1-T4
status        done | blocked | failed
artifact_path frontend/src/views/admin/CourseOversightView.vue ; plan/artifacts/G1-S1-T4/
acceptance    pass | fail  + which criteria
deviations    <or "none">
```
